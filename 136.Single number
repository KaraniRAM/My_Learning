class Solution(object):
    def singleNumber(self, nums):
        for i in range(len(nums)):
            c=nums.count(nums[i])
            if c==1:
                return nums[i]
                
        
