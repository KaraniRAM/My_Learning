method 1
class Solution(object):
    def check(self, nums):
        for i in range (len(nums)):
            g=nums[i:]+nums[:i]
            if g==sorted(g):
                return True
        return False

method 2
class Solution(object):
    def check(self, nums):
        k=sorted(nums)
        for i in range (len(nums)):
            g=nums[i:]+nums[:i]
            if g==k:
                return True
        return False
